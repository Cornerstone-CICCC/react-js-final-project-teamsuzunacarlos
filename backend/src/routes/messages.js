// Message routes
// Should include:
// - GET /messages/:matchId - get conversation for a match
// - POST /messages - send a message (authenticated, via WebSocket primarily)
// - GET /conversations - get all active conversations for user
