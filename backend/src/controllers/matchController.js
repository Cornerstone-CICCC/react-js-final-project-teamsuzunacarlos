// Match business logic
// Functions to handle:
// - getUserMatches(userId)
// - createMatch(sock1Id, sock2Id)
// - acceptMatch(matchId, userId)
// - rejectMatch(matchId, userId)
// - unmatch(matchId, userId)
