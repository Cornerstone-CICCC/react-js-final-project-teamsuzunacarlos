// Sock business logic
// Functions to handle:
// - getAllSocks(filters)
// - getSockById(sockId)
// - createSock(userId, sockData, images)
// - updateSock(sockId, userId, sockData)
// - deleteSock(sockId, userId)
// - getUserSocks(userId)
